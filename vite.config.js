import { defineConfig } from 'vite';

export default defineConfig({
  base: '/divizion-launcher-earth-view/', // Base URL for GitHub Pages
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
  }
});
